/** @type {import('tailwindcss').Config} */

module.exports = {
    content: ['**/*.html'],
    theme: {
        extend: {
            // Faça a extensão dos estilos aqui
        }
    },
    corePlugins: {
        preflight: false
    }
}